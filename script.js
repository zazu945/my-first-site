// (Click tracking  Dynamic animations)
console.log("Connect page loaded successfully!");

const buttons = document.querySelectorAll('.btn');

buttons.forEach(button => {
    button.addEventListener('click', () => {
        // Button click logic
    });
});